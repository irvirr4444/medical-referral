# EMR_URL=
# EMR_USERNAME=
# EMR_PASSWORD=
#
# Create-patient fill does not need TEST_PATIENT_NAME / TEST_PATIENT_ID.
# Synthetic TEST payload lives in synthetic_data.py (or pass JSON later).
#
# Copy these into the repo-root .env file (not committed).
