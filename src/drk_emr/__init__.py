"""DRK EMR automation package.

Subpackages:
- `drk_emr.read_patient` — fetch existing patient dashboard cards by name
- `drk_emr.create_patient` — fill Create Patient / intake form (never submits Create Patient)
- `drk_emr.common` — shared models and redaction helpers
"""
